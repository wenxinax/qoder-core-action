{
  "name": "qoder-core-action",
  "version": "1.0.0",
  "description": "Core logic for calling qoder-cli.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/http-client": "^2.2.3"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.0.0"
  }
}
